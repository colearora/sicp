#lang sicp

;===============================================================================
; Source
;===============================================================================

; cont-frac returns the k-term finite continued fraction.
; (Recursive implementation.)
(define (cont-frac n d k)
  (define (frac i)
    (if (= i k)
        (/ (n k) (d k))
        (/ (n i) (+ (d i)
                    (frac (+ i 1))))))
  (frac 1))

;===============================================================================
; Test
;===============================================================================

; Approximation to 1/phi ~= 1.6180327868852458.
(cont-frac (lambda (x) 1.0)
           (lambda (x) 1.0)
           100) ; 0.6180339887498948

